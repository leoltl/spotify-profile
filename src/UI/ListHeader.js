import styled from "styled-components";

const ListHeader = styled.div`
  display: flex;
  align-items: center;
  h2 {
    font-weight: 600;
  }
  .button {
    margin-left: auto;
  }
`;

export default ListHeader;
